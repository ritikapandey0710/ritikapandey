CONSENT REQUIRED: Database Reset for Ticket Number Feature

To implement human-readable ticket IDs (TKT-00001, etc.) in the Tickets page, we need to add a `ticketNumber` column to the Ticket table. This requires resetting the database, which will destroy all current data and reseed it.

Since you just seeded the database for this task (100 tickets), this reset is safe for your development environment.

Please respond with explicit consent to proceed:
"I consent to resetting the database to add ticketNumber column for human-readable ticket IDs."

After your confirmation, we will execute:
1. `prisma migrate reset --force`
2. `bun run server/seed_tickets.ts`