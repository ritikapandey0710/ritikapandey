[1mdiff --git a/server/src/ai.controller.ts b/server/src/ai.controller.ts[m
[1mindex bfaf521..1c5f9cc 100644[m
[1m--- a/server/src/ai.controller.ts[m
[1m+++ b/server/src/ai.controller.ts[m
[36m@@ -4,9 +4,9 @@[m
 // `GEMINI_API_KEY` and is used ONLY on the server when calling Google's[m
 // generateContent endpoint. It is never sent to, embedded in, or returned to[m
 // the React client. The client only talks to the `/api/ai/polish` endpoint.[m
[31m-[m
[32m+[m[32m//[m
 // Currently-supported Gemini model that is eligible for the free tier.[m
[31m-const GEMINI_MODEL = "gemini-3.6-flash";[m
[32m+[m[32mconst GEMINI_MODEL = "gemini-3.5-flash-lite";[m
 const GEMINI_ENDPOINT = `https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_MODEL}:generateContent`;[m
 [m
 interface GeminiPart {[m
[36m@@ -57,9 +57,15 @@[m [mexport async function polishReply(req: any, res: any) {[m
     });[m
   }[m
 [m
[31m-  const customerFirstName = customerName ? customerName.split(" ")[0] : "there";[m
[31m-  const agent = agentName || "Support Team";[m
[31m-  const ticketSubject = subject ? subject.trim() : "";[m
[32m+[m[32m  // Trim and validate inputs to prevent whitespace issues[m
[32m+[m[32m  const trimmedAgentName = agentName?.trim() || "";[m
[32m+[m[32m  const trimmedCustomerName = customerName?.trim() || "";[m
[32m+[m[32m  const trimmedSubject = subject?.trim() || "";[m
[32m+[m
[32m+[m[32m  const customerFirstName = trimmedCustomerName ? trimmedCustomerName.split(" ")[0] : "there";[m
[32m+[m[32m  // Use trimmed agent name, fallback to "Support Team" if empty or invalid[m
[32m+[m[32m  const agent = trimmedAgentName.length > 0 ? trimmedAgentName : "Support Team";[m
[32m+[m[32m  const ticketSubject = trimmedSubject;[m
 [m
   const prompt = `You are a professional Help Desk agent reviewing a draft reply before sending it to a customer. Your job is to polish the draft into a clear, professional, and natural customer-support response.[m
 [m
[36m@@ -100,6 +106,7 @@[m [mDO NOT include:[m
 [m
 Return ONLY the final customer-ready reply.`;[m
 [m
[32m+[m
   try {[m
     const response = await fetch(`${GEMINI_ENDPOINT}?key=${apiKey}`, {[m
       method: "POST",[m
[36m@@ -125,6 +132,13 @@[m [mReturn ONLY the final customer-ready reply.`;[m
       } catch {[m
         // Keep the default HTTP status message.[m
       }[m
[32m+[m[32m      // Check for 429 (Quota exceeded)[m
[32m+[m[32m      if (response.status === 429) {[m
[32m+[m[32m        console.error("Gemini API quota exceeded:", geminiErrorMessage);[m
[32m+[m[32m        return res[m
[32m+[m[32m          .status(429)[m
[32m+[m[32m          .json({ error: "AI free quota has been reached. Please try again after the quota resets." });[m
[32m+[m[32m      }[m
       console.error("Gemini API error:", response.status, geminiErrorMessage);[m
       return res[m
         .status(500)[m
[36m@@ -153,7 +167,7 @@[m [mReturn ONLY the final customer-ready reply.`;[m
         error:[m
           finishReason === "MAX_TOKENS"[m
             ? "Failed to polish reply: the response was too long and was truncated. Please shorten your reply and try again."[m
[31m-            : "Failed to polish reply: the response was blocked or incomplete. Please try again.",[m
[32m+[m[32m            : "Failed to polish reply: the response was blocked or incomplete. Please try again",[m
       });[m
     }[m
 [m
[36m@@ -164,4 +178,189 @@[m [mReturn ONLY the final customer-ready reply.`;[m
       error: error?.message || "Failed to polish reply",[m
     });[m
   }[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mexport async function summarizeTicket(req: any, res: any) {[m
[32m+[m[32m  const { subject, description, customerName, status, priority, category, assigneeId, replies } = req.body;[m
[32m+[m
[32m+[m[32m  if (!subject || !description || !replies || replies.length === 0) {[m
[32m+[m[32m    return res.status(400).json({ error: "Ticket data and conversation history are required" });[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  // Read the key fresh on every request so dev reloads pick up changes.[m
[32m+[m[32m  const apiKey = process.env.GEMINI_API_KEY;[m
[32m+[m
[32m+[m[32m  if (!apiKey) {[m
[32m+[m[32m    console.error("summarizeTicket: GEMINI_API_KEY is not set on the server");[m
[32m+[m[32m    return res.status(500).json({[m
[32m+[m[32m      error:[m
[32m+[m[32m        "Ticket summarization is not available right now. The Gemini API key is not configured on the server. Please contact your administrator.",[m
[32m+[m[32m    });[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  const customerFirstName = customerName ? customerName.split(" ")[0] : "there";[m
[32m+[m[32m  const ticketSubject = subject.trim();[m
[32m+[m[32m  const ticketDescription = description?.trim() || "";[m
[32m+[m[32m  const ticketStatus = status?.trim() || "";[m
[32m+[m[32m  const ticketPriority = priority?.trim() || "";[m
[32m+[m[32m  const ticketCategory = category?.trim() || "";[m
[32m+[m[32m  // Use assignee name if available, otherwise "Unassigned"[m
[32m+[m[32m  let assignedAgent = "Unassigned";[m
[32m+[m[32m  if (assigneeId) {[m
[32m+[m[32m    // Simply use the assigneeId as the agent name for display purposes[m
[32m+[m[32m    // The frontend will show the actual user name from the ticket data[m
[32m+[m[32m    assignedAgent = `Assignee ${assigneeId.substring(0, 8)}...`;[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  // Build conversation history from replies[m
[32m+[m[32m  const conversationHistory = replies[m
[32m+[m[32m    .map((reply: any) => {[m
[32m+[m[32m      const authorName = reply.author?.name || reply.senderType === 'AGENT' ? 'Agent' : 'Customer';[m
[32m+[m[32m      const authorType = reply.senderType || (reply.author?.name ? (reply.author.name.includes('Agent') ? 'AGENT' : 'CUSTOMER') : 'Unknown');[m
[32m+[m[32m      return `${authorType}: ${reply.body}`;[m
[32m+[m[32m    })[m
[32m+[m[32m    .join('\n');[m
[32m+[m
[32m+[m[32m  const prompt = `You are summarizing a Help Desk ticket and its complete conversation history for a support agent.[m
[32m+[m
[32m+[m[32mTICKET SUBJECT:[m
[32m+[m[32m${ticketSubject}[m
[32m+[m
[32m+[m[32mTICKET DESCRIPTION:[m
[32m+[m[32m${ticketDescription}[m
[32m+[m
[32m+[m[32mTICKET STATUS:[m
[32m+[m[32m${ticketStatus}[m
[32m+[m
[32m+[m[32mTICKET PRIORITY:[m
[32m+[m[32m${ticketPriority}[m
[32m+[m
[32m+[m[32mTICKET CATEGORY:[m
[32m+[m[32m${ticketCategory}[m
[32m+[m
[32m+[m[32mASSIGNED AGENT:[m
[32m+[m[32m${assignedAgent}[m
[32m+[m
[32m+[m[32mCONVERSATION HISTORY (complete, chronological):[m
[32m+[m[32m${conversationHistory}[m
[32m+[m
[32m+[m[32mINSTRUCTIONS:[m
[32m+[m[32m1. Understand the ticket and the complete conversation before summarizing.[m
[32m+[m[32m2. Read ALL customer and agent messages in the conversation.[m
[32m+[m[32m3. Customer and agent messages may alternate - identify who said what.[m
[32m+[m[32m4. Preserve important facts from both the ticket and conversation.[m
[32m+[m[32m5. Do NOT invent information, troubleshooting steps, resolutions, dates, promises, or customer information.[m
[32m+[m[32m6. Distinguish customer statements from agent actions.[m
[32m+[m[32m7. Identify what has already been attempted.[m
[32m+[m[32m8. Identify the current state based on the conversation.[m
[32m+[m[32m9. Identify unresolved/pending items.[m
[32m+[m[32m10. Use ONLY information present in the ticket and conversation.[m
[32m+[m[32m11. Produce a professional, concise but complete support summary.[m
[32m+[m
[32m+[m[32mFORMAT - Return only the summary in this exact structure:[m
[32m+[m
[32m+[m[32mTicket Summary[m
[32m+[m
[32m+[m[32mIssue:[m
[32m+[m[32m[short explanation of what the customer originally reported and what problem they are experiencing][m
[32m+[m
[32m+[m[32mCustomer Report:[m
[32m+[m[32m[what the cust